# HyperCore Documentation Source Conflicts

These are publication blockers where they affect names, wire behavior, or availability.

| Conflict | Conflicting sources | Current evidence | Decision required | Publication impact | Likely owner |
| --- | --- | --- | --- | --- | --- |
| Internal build versus obsolete white-label framing | Older white-label documents; newer internal build maps v3/v4 | Newer maps state an Alchemy internal build. No public document is created from older strategy material. | None for strategy; archive or label obsolete docs. | Prevents inaccurate public positioning. | Product leadership |
| P0 WebSocket scope | Vetted sheet lists `l2Book`, `l2BookDiff`, `allFills`, `userFills`, `bbo`; v4 says native passthrough proxy for 22 native subscriptions | The current map does not establish that custom P0 names are registered or publicly available. | Final P0 operation list and custom-vs-passthrough decision. | Blocks all stream pages. | Streaming product + engineering |
| Dedicated streams versus broad filters | Vetted sheet favors purpose-built fill/order streams; native/other provider surfaces use broad streams and filters | No Alchemy subscription registration or schema is available. | Canonical public stream model and filter semantics. | Blocks fill, builder, liquidation, and order-update pages. | Streaming product |
| Singular/plural and variant names | `l4Book`/`l4BookUpdates`/`l4BookUpdatesAll`; `fundingRates`/`userFundings`; `userNonFundingLedgerEvents`/`userNonFundingLedgerUpdates`; `webData2`/`webData3`; `settledOutcome`/`settledOutcomes*` | These are native, provider, or inventory names, not canonical Alchemy names. | Naming registry backed by code. | Blocks reference slugs and examples. | API design |
| REST snapshot versus stream names | `l2Book`, `allFills`, `builderFills`, candles, and others appear in both inventory categories | No Alchemy transport definitions are present. | Whether operations are separate routes/subscriptions and their names. | Blocks cross-linking and coverage claims. | API design |
| `outcomeMetaUpdates` | Inventory identifies it as disputed; newest v4 map says no dedicated native subscription exists | The newest map is stronger planning evidence but still not implementation. | Confirm against current Hyperliquid integration and Alchemy scope. | Must not be published. | Hyperliquid integration owner |
| `StreamBlocks` and gRPC phase | Vetted sheet P1; inventory P0; v4 P4 gated | No proto or service exists. | Launch decision and protobuf approval. | No gRPC documentation can be published. | Data-platform + product |
| JSON-RPC batch phase and caps | Inventory marks P3 and lists caps; v4 places batch APIs in P2 | Both are plans; no registration or schema exists. | Canonical phase, names, and validated caps. | Blocks block/backfill references. | Data-platform |
| Block fields and finality | Build maps propose raw blocks, actions, and replica commands; no block schema exists | No parser, fixture, or API response found. | Block model, completeness, missing-block, and finality contract. | Blocks block guide and recovery workflow. | Data engineering |
| Historical coverage | Build maps prescribe immediate capture and later archives/shares; no evidence of active capture, retention, or exported data | Node file-writing and retention are explicitly open in v4. | Historical availability matrix approved by engineering. | Blocks all historical coverage claims. | Data engineering |
| Write API release scope | v4 includes P0 prototype/P2 hardening; inventory places writes in P3 | Separate local builder-code repo cannot establish HyperCore release scope. | Final public write scope and security review. | No write docs. | Trading API owner |
| `portfolio` versus `portfolioState` and enriched `++` labels | Inventory and planning use overlapping names and informal enrichment suffixes | `++` is planning notation, not a public method name. | Canonical REST naming and schema. | No enriched REST pages. | API design |
| Provider-only methods | Broad inventory includes many provider-specific custom endpoints and gRPC methods | No evidence establishes Alchemy equivalents. | Explicit analogue decisions where desired. | Omit from public docs. | Product |
| Methods in one source but not the vetted sheet | Broad inventory includes unscored native, custom, gRPC, and batch methods | The priority sheet is intentionally not exhaustive. | Per-method launch decisions. | Covered internally; not public by default. | Product + engineering |
| Vetted P0-P2 methods absent from implementation | All P0-P2 streams and several data surfaces | No server implementation, schema, test, or fixture exists in the available workspace. | Link implementation evidence or retain as requirement. | No public contract. | Engineering lead |
